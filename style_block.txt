
>   <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  
      :root {
        --bg: #f0f5fb;
        --bg2: #e8eef8;
        --card: #ffffff;
        --card2: #f8fafd;
        --border: rgba(0,0,0,.08);
        --accent: #10b981;
        --accent2: #059669;
        --paypal: #003087;
        --paypal2: #009cde;
        --text: #0f172a;
        --muted: #64748b;
        --muted2: #475569;
        --plus: #d97706;
        --social: #6366f1;
        --shadow: 0 2px 16px rgba(0,0,0,.07);
      }
  
      body {
        font-family: 'Inter', sans-serif;
        background: var(--bg);
        color: var(--text);
        min-height: 100vh;
      }
  
      /* тФАтФАтФА NAVBAR тФАтФАтФА */
      .navbar {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0 32px;
        height: 64px;
        background: rgba(255,255,255,.92);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid var(--border);
        position: sticky;
        top: 0;
        z-index: 100;
      }
      .brand {
        font-size: 20px;
        font-weight: 800;
        color: var(--accent);
        text-decoration: none;
        letter-spacing: -.3px;
      }
      .nav-links { display: flex; gap: 4px; }
      .nav-link {
        color: var(--muted2);
        text-decoration: none;
        font-size: 13px;
        font-weight: 600;
        padding: 7px 14px;
        border-radius: 8px;
        transition: all .15s;
      }
      .nav-link:hover { color: var(--text); background: rgba(255,255,255,.06); }
      .nav-link.active { color: var(--text); background: rgba(255,255,255,.08); }
      .nav-user {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 13px;
        font-weight: 600;
        color: var(--muted2);
      }
      .nav-avatar {
        width: 34px; height: 34px;
        border-radius: 50%;
        background: linear-gradient(135deg, var(--accent), var(--paypal2));
        display: flex; align-items: center; justify-content: center;
        font-size: 13px; font-weight: 800; color: #fff;
        cursor: pointer;
      }
  
      /* тФАтФАтФА LAYOUT тФАтФАтФА */
      .page { max-width: 1080px; margin: 0 auto; padding: 40px 20px 80px; }
  
      .page-hero {
        text-align: center;
        margin-bottom: 48px;
      }
      .page-hero h1 {
        font-size: 40px;
        font-weight: 800;
        background: linear-gradient(135deg, #f1f5f9 30%, #4ade80);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        line-height: 1.15;
        margin-bottom: 12px;
      }
      .page-hero p {
        color: var(--muted2);
        font-size: 16px;
        max-width: 480px;
        margin: 0 auto;
      }
  
      /* тФАтФАтФА STATUS BAR тФАтФАтФА */
      .status-bar {
        display: flex;
        align-items: center;
        gap: 16px;
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 14px;
        padding: 16px 24px;
        margin-bottom: 32px;
        box-shadow: var(--shadow);
      }
      .status-icon {
        width: 44px; height: 44px;
        border-radius: 12px;
        background: linear-gradient(135deg, rgba(74,222,128,.2), rgba(34,197,94,.1));
        border: 1px solid rgba(74,222,128,.2);
        display: flex; align-items: center; justify-content: center;
        font-size: 20px;
        flex-shrink: 0;
      }
      .status-info { flex: 1; }
      .status-plan {
        font-size: 15px;
        font-weight: 700;
        color: var(--text);
      }
      .status-detail {
        font-size: 12px;
        color: var(--muted);
        margin-top: 2px;
      }
      .status-badge {
        font-size: 11px;
        font-weight: 700;
        padding: 4px 10px;
        border-radius: 20px;
        background: rgba(74,222,128,.12);
        color: var(--accent);
        border: 1px solid rgba(74,222,128,.2);
      }
      .status-badge.plus { background: rgba(245,158,11,.12); color: var(--plus); border-color: rgba(245,158,11,.2); }
      .status-badge.social { background: rgba(129,140,248,.12); color: var(--social); border-color: 
rgba(129,140,248,.2); }
  
      /* тФАтФАтФА PLANS GRID тФАтФАтФА */
      .plans-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
        margin-bottom: 32px;
      }
      @media (max-width: 768px) { .plans-grid { grid-template-columns: 1fr; } }
  
      .plan-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 18px;
        box-shadow: var(--shadow);
        padding: 24px;
        display: flex;
        flex-direction: column;
        gap: 16px;
        transition: border-color .2s, transform .2s;
        position: relative;
        overflow: hidden;
      }
      .plan-card:hover { border-color: rgba(255,255,255,.15); transform: translateY(-2px); }
      .plan-card.popular {
        border-color: rgba(245,158,11,.35);
        background: linear-gradient(145deg, #1a2540, #1e2c40);
      }
      .plan-card.active-plan { border-color: rgba(74,222,128,.35); }
  
      .plan-popular-badge {
        position: absolute; top: 16px; right: 16px;
        font-size: 10px; font-weight: 800;
        padding: 3px 9px; border-radius: 20px;
        background: linear-gradient(90deg, var(--plus), #f97316);
        color: #fff;
        letter-spacing: .5px;
        text-transform: uppercase;
      }
  
      .plan-name {
        font-size: 13px; font-weight: 700;
        color: var(--muted2);
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      .plan-price {
        font-size: 34px; font-weight: 800;
        color: var(--text);
        line-height: 1;
      }
      .plan-price span { font-size: 14px; font-weight: 500; color: var(--muted); }
  
      .plan-features { list-style: none; display: flex; flex-direction: column; gap: 8px; flex: 1; }
      .plan-features li {
        display: flex; align-items: flex-start; gap: 8px;
        font-size: 13px; color: var(--muted2); line-height: 1.4;
      }
      .plan-features li::before { content: 'тЬУ'; color: var(--accent); font-weight: 700; flex-shrink: 0; }
      .plan-card.popular .plan-features li::before { color: var(--plus); }
  
      .plan-btn {
        width: 100%;
        padding: 12px;
        border-radius: 10px;
        border: none;
        cursor: pointer;
        font-size: 13px;
        font-weight: 700;
        font-family: inherit;
        transition: all .2s;
      }
      .plan-btn.free-btn {
        background: rgba(255,255,255,.06);
        color: var(--muted2);
        border: 1px solid var(--border);
      }
      .plan-btn.free-btn:hover { background: rgba(255,255,255,.1); color: var(--text); }
      .plan-btn.social-btn {
        background: rgba(129,140,248,.12);
        color: var(--social);
        border: 1px solid rgba(129,140,248,.25);
      }
      .plan-btn.social-btn:hover { background: rgba(129,140,248,.22); }
      .plan-btn.paypal-btn {
        background: linear-gradient(135deg, var(--paypal), #0070ba);
        color: #fff;
        display: flex; align-items: center; justify-content: center; gap: 8px;
      }
      .plan-btn.paypal-btn:hover { background: linear-gradient(135deg, #004ab3, #0070ba); transform: scale(1.02); }
      .plan-btn:disabled {
        opacity: .6; cursor: not-allowed;
        transform: none !important;
      }
      .plan-btn.active-btn {
        background: rgba(74,222,128,.1);
        color: var(--accent);
        border: 1px solid rgba(74,222,128,.25);
        cursor: default;
      }
  
      /* тФАтФАтФА PAYPAL LOGO тФАтФАтФА */
      .paypal-logo {
        display: inline-flex; align-items: center; gap: 3px;
        font-weight: 800; font-size: 14px;
      }
      .paypal-logo .pay { color: #009cde; }
      .paypal-logo .pal { color: #003087; }
  
      /* тФАтФАтФА SOCIAL FORM тФАтФАтФА */
      .social-reason-box {
        background: var(--card);
        border: 1px solid rgba(129,140,248,.25);
        border-radius: 14px;
        padding: 20px;
        margin-bottom: 32px;
        display: none;
      }
      .social-reason-box.show { display: block; }
      .social-reason-box label {
        display: block;
        font-size: 13px; font-weight: 700;
        color: var(--muted2);
        margin-bottom: 8px;
      }
      .social-reason-box textarea {
        width: 100%;
        background: rgba(255,255,255,.04);
        border: 1px solid var(--border);
        border-radius: 8px;
        color: var(--text);
        font-family: inherit;
        font-size: 14px;
        padding: 10px 14px;
        resize: vertical;
        min-height: 80px;
      }
      .social-reason-box textarea:focus { outline: none; border-color: var(--social); }
  
      /* тФАтФАтФА RECEIPT тФАтФАтФА */
      .receipt-section { display: none; margin-bottom: 32px; }
      .receipt-section.show { display: block; }
      .receipt-card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 14px;
        overflow: hidden;
      }
      .receipt-header {
        background: linear-gradient(90deg, var(--card2), #1a2540);
        padding: 14px 20px;
        display: flex; align-items: center; gap: 10px;
        border-bottom: 1px solid var(--border);
      }
      .receipt-header .title { font-size: 12px; font-weight: 800; color: var(--muted2); letter-spacing: 1px; 
text-transform: uppercase; }
      .receipt-body { padding: 4px 0; }
      .receipt-row {
        display: flex; justify-content: space-between;
        padding: 10px 20px;
        font-size: 13px;
        border-bottom: 1px solid var(--border);
      }
      .receipt-row:last-child { border-bottom: none; }
      .receipt-row .label { color: var(--muted); }
      .receipt-row .value { color: var(--text); font-weight: 600; }
      .receipt-row .value.success { color: var(--accent); }
  
      /* тФАтФАтФА TOAST тФАтФАтФА */
      #toast {
        position: fixed;
        bottom: 28px; right: 28px;
        background: #ffffff;
        border: 1px solid var(--border);
        box-shadow: 0 4px 20px rgba(0,0,0,.12);
        border-radius: 12px;
        padding: 14px 20px;
        font-size: 14px; font-weight: 600;
        color: var(--text);
        max-width: 340px;
        z-index: 9999;
        transform: translateY(20px);
        opacity: 0;
        transition: all .3s ease;
        display: flex; align-items: center; gap: 10px;
      }
      #toast.show { transform: translateY(0); opacity: 1; }
      #toast.success { border-color: rgba(74,222,128,.3); }
      #toast.error { border-color: rgba(239,68,68,.3); }
      #toast .toast-icon { font-size: 18px; flex-shrink: 0; }
  
      /* тФАтФАтФА LOADING тФАтФАтФА */
      .loading-state {
        text-align: center;
        padding: 60px 20px;
        color: var(--muted);
      }
      .spinner {
        width: 36px; height: 36px;
        border: 3px solid rgba(0,0,0,.1);
        border-top-color: var(--accent);
        border-radius: 50%;
        animation: spin .7s linear infinite;
        margin: 0 auto 16px;
      }
      @keyframes spin { to { transform: rotate(360deg); } }
  


